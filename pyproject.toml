[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tsbhb"
version = "0.1.0"
description = "TSB-HB supplement code (modularized)"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

